import React from "react"
import { useNavigate } from "react-router-dom";

const Room: React.FC = () => {
    return (
        <div>
            Room
        </div>
    );
};

export default Room;